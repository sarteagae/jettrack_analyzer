# Jet Track Correlation Analyzer
This code uses HIForest or Skims as input (same format). It is completelly CMSSW independent (but it depends on ROOT version).

## Code structure

## Instructions to run
